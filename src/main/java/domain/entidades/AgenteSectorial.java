package domain.entidades;

import java.util.List;

/*Cada agente pertenece a un solo sector territorial, los cuales pueden ser Departamentos (o Municipios) o
Provincias.
Se debe considerar que la HC total de un sector territorial es el resultado de la sumatoria de las HC totales
de cada Organización que se encuentre dentro de ese sector.
*/
public class AgenteSectorial { //todo
    /*
    private Sector sector; // puede ser er Departamentos (o Municipios)  o Provincias

    public Float calcularHCSectorial(){ // capaz es mas facul hacer un filter de todas las orgs que tengan ese sector
        int sumatoria = 0;
        return this.sector.getOrganizaciones().stream.mapToDouble(org->org.calculoHC()).sum();
    }

    }
    */
}
